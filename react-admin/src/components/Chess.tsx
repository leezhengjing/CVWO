import { Chessboard } from "react-chessboard";

const Chess = () => {
    return (
        <div>
            <Chessboard id="BasicBoard" boardWidth={560} />
        </div>
    )
}

export default Chess;